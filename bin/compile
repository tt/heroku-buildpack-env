#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

echo "-----> Displaying compile environment."
env
echo "-----> Done."

echo "-----> Displaying process environment."
ps a -o pid | sed 1d | xargs -I % -n 1 cat /proc/%/environ
echo "-----> Done."

# abort compilation
exit 1
